# ─── Database ────────────────────────────────────────────────────────────────
# SQLite (default, good for local dev)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/crifcan2_poc

# PostgreSQL example (recommended for production)
# DATABASE_URL=postgresql://user:password@localhost:5432/regulatory_db

# ─── Security ─────────────────────────────────────────────────────────────────
# Generate a strong key: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=changeme

# ─── App ──────────────────────────────────────────────────────────────────────
APP_TITLE=Regulatory Service
APP_VERSION=1.0.0

# ─── CORS ─────────────────────────────────────────────────────────────────────
# Comma-separated list of allowed origins, or * for all
# ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8080,http://192.168.100.238:3000,
ALLOWED_ORIGINS=*
